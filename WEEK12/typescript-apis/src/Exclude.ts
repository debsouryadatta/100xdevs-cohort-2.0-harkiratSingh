// type event = 'click' | 'scroll' | 'mousemove'

// type ExcludeEvent = Exclude<event, 'scroll'>;

// const handleEvent = (event: ExcludeEvent) => {
//     console.log(event);
// }

// handleEvent('scroll'); // This is not allowed now!